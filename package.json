{
  "name": "claude-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "claude-monitor": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/claude-monitor"
  },
  "dependencies": {
    "chokidar": "^4.0.0"
  }
}
